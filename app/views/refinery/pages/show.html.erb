<div class="slide parallax-window" data-parallax="scroll" data-image-src="<%= image_url(header_image(@page)) %>">
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <h1><%= @page.title %></h1>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-xs-10 col-xs-offset-1 col-md-8 col-md-offset-1 content">
    <%= raw @page.content_for(:body) %>
  </div>
</div>
